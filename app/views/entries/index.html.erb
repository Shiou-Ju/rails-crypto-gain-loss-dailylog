<p style="color: green"><%= notice %></p>

<% content_for :title do%>
  Gains & Lost Calculator &ndash; <%= DateTime.now %>
<%end%>

<div id="entries">
  <% @entries.each do |entry| %>
    <%= render entry %>
    <p>
      <%= link_to "Show this entry", entry %>
    </p>
  <% end %>
</div>

<%= link_to "New entry", new_entry_path %>
