<%= form_with(model: entry) do |form| %>
  <% if entry.errors.any? %>
    <div style="message is-danger">
      <h2><%= pluralize(entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

    <div class="message-body">
        <ul>
         <% entry.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div>
    <%= form.label :coin_type, class: 'label' %>
    <%= form.text_field :coin_type %>
  </div>

  <div>
    <%= form.label :purchase_price, class: 'label' %>
    <%= form.number_field :purchase_price %>
  </div>

  <div>
    <%= form.label :selling_price, class: 'label' %>
    <%= form.number_field :selling_price %>
  </div>

  <div>
    <%= form.submit class: 'button is-success' %>
  </div>
<% end %>
