<%= form_with(model: entry) do |form| %>
  <% if entry.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
        <% entry.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :coin_type, style: "display: block" %>
    <%= form.text_field :coin_type %>
  </div>

  <div>
    <%= form.label :purchase_price, style: "display: block" %>
    <%= form.number_field :purchase_price %>
  </div>

  <div>
    <%= form.label :selling_price, style: "display: block" %>
    <%= form.number_field :selling_price %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
